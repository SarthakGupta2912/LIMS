name: invoice_management_system
description: "Invoice generator and management system"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  window_manager: ^0.5.1
  shared_preferences: ^2.5.4
  path_provider: ^2.1.5
  filesystem_picker: ^4.1.0
  pdf: ^3.11.3
  printing: ^5.14.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
